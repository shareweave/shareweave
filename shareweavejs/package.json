{
  "name": "shareweavejs",
  "version": "1.0.0",
  "description": "the js library for building with shareweave",
  "private": true,
  "main": "build/src/index.js",
  "typings": "build/src/index.d.ts",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@ethersproject/providers": "^5.5.2",
    "@types/jest": "^27.4.0",
    "eth-testing": "^1.1.0-alpha.6",
    "jest": "^27.4.7",
    "ts-jest": "^27.1.3",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "prettier --ignore-path ../.gitignore --check --plugin-search-dir=.. . && eslint --ignore-path ../.gitignore .",
    "format": "prettier --ignore-path ../.gitignore --write --plugin-search-dir=.. ."
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "ethers": "^5.5.3"
  }
}