{
  "private": "true",
  "workspaces": [
    "frontend",
    "shareweavejs"
  ],
  "scripts": {
    "format": "prettier --write .",
    "lint": "prettier --ignore-path .gitignore --check --plugin-search-dir=.. . && eslint --ignore-path .gitignore .",
    "test": "cd shareweavejs && yarn test",
    "test:watch": "cd shareweavejs && yarn test --watch",
    "build": "cd frontend && yarn build-webcomponents && cd ../shareweavejs && yarn build && cd ../frontend && svelte-kit build",
    "dev": "cd frontend && yarn build-webcomponents:watch & cd shareweavejs && yarn build:watch & cd frontend && svelte-kit dev"
  },
  "dependencies": {
    "ethers": "^5.5.3"
  },
  "devDependencies": {
    "prettier-plugin-svelte": "^2.6.0"
  }
}